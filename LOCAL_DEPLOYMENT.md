# Local Deployment

Use the production build locally:

```bash
npm run deploy:local
```

The app will be available at:

```text
http://localhost:4173
```

If you only want to serve an existing build:

```bash
npm run preview:local
```
